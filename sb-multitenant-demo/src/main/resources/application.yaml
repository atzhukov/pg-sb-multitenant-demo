spring:
  application:
    name: sb-multitenant-demo
  datasource:
    url: ${DB_URL}
    username: ${APP_POSTGRES_USERNAME}
    password: ${APP_POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
